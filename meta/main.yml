---
galaxy_info:
  author: Ona Engineering
  description: Installs collectd
  min_ansible_version: 2.0
  license: Apache2
  platforms:
    - name: Ubuntu
      versions:
        - trusty
        - xenial
  categories:
    - collectd

dependencies:
 - role: geerlingguy.java
   become: true
   become_user: "root"
   java_packages: "{{ collectd_java_packages }}"
   when: collect_install_java
   tags:
    - java